apiVersion: v1
kind: PersistentVolume
metadata:
  name: tekton-results
  namespace: tekton-pipelines
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /logs/
